export type Pokemon = {
  name: string;
  url: string;
};

export type PokemonImage = {
  image: string;
};
export type PokemonName = {
  PokemonName: string;
  setScore: React.Dispatch<React.SetStateAction<number>>;
};
